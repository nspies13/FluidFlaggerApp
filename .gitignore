__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
.env
*.joblib
models/
data/training_templates/
.cache/
.venv/
.pytest_cache/
.ruff_cache/
*.tmp
*.zip
data/self_test_log.db*
fluidflagger-inference.tar.gz
dev/exemplar_bank/features.csv
dev/exemplar_bank/labels.npy

# macOS
.DS_Store

# LaTeX build artifacts
*.aux
*.fdb_latexmk
*.fls
*.synctex.gz
*.spl

# Word lock files
~$*

# Manuscript artifacts (LaTeX sources, figures, submission documents).  These
# live on disk under paper/files/ but are deliberately untracked: they are
# large binaries that the Hugging Face Hub rejects outside LFS/Xet, and the
# figures and metrics tables are regenerated by paper/validate_models.py.
paper/files/

# Large validation sets (kept out of VCS; used by paper/validate_models.py)
data/*_validation_set.csv
